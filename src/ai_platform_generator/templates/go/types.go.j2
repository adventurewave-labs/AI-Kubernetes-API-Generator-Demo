// Code generated by ai-platform-generator. DO NOT EDIT.
// Regenerate via: ai-platform-generator generate ...

// Package {{ version_pkg }} contains API Schema definitions for the
// {{ group }} {{ version_pkg }} API group.
// +kubebuilder:object:generate=true
// +groupName={{ group }}
package {{ version_pkg }}

import (
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
	"k8s.io/apimachinery/pkg/runtime"
	"sigs.k8s.io/controller-runtime/pkg/scheme"
)

// GroupVersion is the API GroupVersion used to register these objects.
var GroupVersion = metav1.GroupVersion{Group: "{{ group }}", Version: "{{ version_pkg }}"}

// SchemeBuilder is used to add go types to the GroupVersionKind scheme.
var SchemeBuilder = &scheme.Builder{GroupVersion: schemaGV()}

// AddToScheme registers this API group's types with a scheme.
var AddToScheme = SchemeBuilder.AddToScheme

func schemaGV() runtime.GroupVersioner {
	return runtime.GroupVersioner(metav1.GroupVersion{
		Group:   "{{ group }}",
		Version: "{{ version_pkg }}",
	})
}

// {{ kind }}Spec defines the desired state of {{ kind }}.
type {{ kind }}Spec struct {
{%- for prop in properties %}
	// {{ prop.description }}
	{%- if prop.required %}
	// +kubebuilder:validation:Required
	{%- else %}
	// +optional
	{%- endif %}
	{{ prop.go_field }} {{ prop.go_type }} `json:"{{ prop.json_tag }}"`
{%- endfor %}
}

// {{ kind }}Status defines the observed state of {{ kind }}.
type {{ kind }}Status struct {
	// ObservedGeneration is the most recent .metadata.generation reconciled
	// by the controller.
	ObservedGeneration int64 `json:"observedGeneration,omitempty"`

	// Conditions represent the latest available observations of the
	// resource's state.
	// +patchMergeKey=type
	// +patchStrategy=merge
	Conditions []metav1.Condition `json:"conditions,omitempty" patchMergeKey:"type" patchStrategy:"merge"`
}

// +kubebuilder:object:root=true
// +kubebuilder:subresource:status

// {{ kind }} is the Schema for the {{ kind_plural }} API.
type {{ kind }} struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   {{ kind }}Spec   `json:"spec,omitempty"`
	Status {{ kind }}Status `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// {{ kind }}List contains a list of {{ kind }}.
type {{ kind }}List struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []{{ kind }} `json:"items"`
}

func init() {
	SchemeBuilder.Register(&{{ kind }}{}, &{{ kind }}List{})
}
