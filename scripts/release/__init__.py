"""Release-time helper scripts for ai-platform-generator.

This module is intentionally tiny and dependency-light — it must run with only
the Python standard library so it can be invoked from CI before any project
dependencies are installed.
"""
